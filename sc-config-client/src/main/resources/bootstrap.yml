spring:
  application:
    name: configclient
  cloud:
    config:
      discovery:
        enabled: true
        service-id: configserver
      profile: dev
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
      


      
      